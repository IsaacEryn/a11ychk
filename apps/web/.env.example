# ── Supabase (https://supabase.com/dashboard → 프로젝트 → Settings → API) ──
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
# 서버 전용 — 절대 클라이언트/저장소에 노출 금지
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# ── 내부 API 보호용 랜덤 시크릿 (openssl rand -hex 32 로 생성) ──
INTERNAL_API_SECRET=change-me

# ── 정기 스캔 크론 보호용 (Vercel Cron이 Authorization 헤더로 자동 전송) ──
CRON_SECRET=change-me-too

# ── (선택) GitHub 저장소 트래픽 수집용 토큰 — repo-stats 크론 ──
# 저장소 push 권한 계정의 fine-grained PAT(Administration: read) 또는 classic repo scope.
# 없으면 트래픽(조회·클론)은 건너뛰고 스타·포크만 기록.
# GITHUB_STATS_TOKEN=github_pat_...

# ── 로케일 맞춤 인증 메일 (Supabase Send Email Hook) ──
# RESEND_API_KEY: Resend API 키 (re_...)
# ADMIN_ALERT_EMAIL: 새 서버 오류 이메일 알림 수신 주소 (미설정 시 알림 생략)
# SEND_EMAIL_HOOK_SECRET: Supabase에서 Send Email 훅 등록 시 발급되는 v1,whsec_… 시크릿
# RESEND_API_KEY=re_...
# ADMIN_ALERT_EMAIL=admin@example.com
# SEND_EMAIL_HOOK_SECRET=v1,whsec_...

# ── 인증 남용 방지 CAPTCHA (Cloudflare Turnstile) ──
# 사이트 키(공개)는 프론트에, 시크릿 키는 Supabase Auth → Settings → Captcha에 입력.
# TURNSTILE_SECRET_KEY: 자체 엔드포인트(비로그인 맛보기 검사) 서버 검증용 — 사이트 키를
#   설정했다면 함께 설정할 것(없으면 맛보기 검사가 설정 오류로 비활성).
# 미설정 시 CAPTCHA 비활성(위젯 렌더 안 됨).
# NEXT_PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAA...
# TURNSTILE_SECRET_KEY=0x4AAAAAA...

# ── 관리자 보안 (선택 — 미설정 시 기본 동작) ──
# ADMIN_PATH_SLUG: 관리자 경로를 /{slug}로 은닉 (소문자 영숫자·하이픈 4~64자, 점 금지).
#   설정 시 /admin 직접 접근은 404. 미설정이면 기존 /admin 그대로. NEXT_PUBLIC_ 금지!
# ADMIN_IDLE_MINUTES: 관리자 무활동 자동 로그아웃(분, 기본 20)
# SESSION_MAX_HOURS: 전 회원 세션 절대 유지 시간(시간, 기본 24)
# ADMIN_PATH_SLUG=
# ADMIN_IDLE_MINUTES=20
# SESSION_MAX_HOURS=24

# ── 사이트 URL (로컬: http://localhost:3000, 운영: https://a11ychk.com) ──
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ── (선택) 로컬 개발용 크롬 실행 파일 경로 — 미지정 시 playwright 설치본 사용 ──
# A11YCHK_CHROME_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome

# ── 검사 큐·동시성 (선택 — 미설정 시 기본값) ──
# 전역 동시 실행 상한 (기본 3, 문제 시 1로 백오프)
# A11YCHK_MAX_CONCURRENT_SCANS=3
# 내부 run-scan 엔드포인트 베이스 URL (배포에선 VERCEL_URL 자동, 로컬 큐 테스트 시 지정)
# A11YCHK_INTERNAL_BASE_URL=http://localhost:3000

# ── 랜딩 "실제 예시 보고서" 데모 (선택) — /demo가 이 scan id의 공유 링크로 302 ──
# NEXT_PUBLIC_DEMO_REPORT_URL=https://www.a11ychk.com/ko/scans/<scan-id>/report?token=...

# Google Tag Manager (GA4) — 설정 시에만 스니펫 삽입 + CSP 허용 (미설정 = 분석 없음)
NEXT_PUBLIC_GTM_ID=
