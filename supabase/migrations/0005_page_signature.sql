-- ═══════════════════════════════════════════════════════════════
-- 사이트 수준 검사(Phase C)를 위한 페이지 시그니처 저장
-- 단일 페이지 재검사 후 재집계 시 제목 유일성·일관된 내비게이션·여러 방법
-- 판정을 다시 계산하려면 페이지별 시그니처(제목·내비 링크·검색/사이트맵 유무)가 필요하다.
-- 실행: Supabase Dashboard → SQL Editor
-- ═══════════════════════════════════════════════════════════════

alter table public.scan_pages
  add column if not exists signature jsonb;
